This image is based on the official CRUX Docker image with the following additions:


+ prt-get sysup on the existing packages

+ create local user "crux" (password: welcome)

+ starts ssh daemon by default
